---
milestone: v0.0.6
milestone_name: phase-1-finish
audited: 2026-05-16
auditor: gsd-audit-milestone (opus, manual orchestrator)
status: gaps_found
scores:
  requirements: 12/41 fully satisfied (8 partial + 21 unsatisfied — see updated counts after integration findings folded in)
  phases: 3/7 have VERIFICATION.md (1, 2, 3); phase 5 has SUMMARY but no VERIFICATION; phases 4, 6, 7 have neither
  integration: "3 BLOCK + 6 FLAG + 7 PASS (.planning/INTEGRATION-CHECK-v0.0.6.md, 170 lines)"
  flows: not directly verified by audit (each phase's own E2E covers its slice)
  nyquist: 0/7 phases have VALIDATION.md (all MISSING — `workflow.nyquist_validation: true`)
overall_assessment: |
  **Updated 2026-05-16 after integration checker landed** — implementation is broadly
  complete on disk, BUT the cross-phase integration check surfaced THREE real BLOCKers
  that are not bookkeeping:

    BLOCK-1: `scripts/install.sh:216` trailing message still points to stale
             `docs/RESUME.md` for the dylib-swap recipe. Phase 5 updated the librime-fork
             installer trailing messages but missed the *primary* user-facing installer
             — new users see a stale path. HARDEN-06 / DOCS-04 / DOCS-07 partially broken.
    BLOCK-2: `scripts/install-windows.ps1` has ZERO `--uninstall` flag matches. mac +
             linux both implement it. STATE.md claims "uninstall flags added" but Phase 6
             only delivered 2 of 3 OSes. DOCS-04 "3-OS parity" claim is false.
    BLOCK-3: `docs/DECISION-GATE-CRITERIA.md` first commit is `e1d1a7a` 2026-05-11
             14:27 — TWO DAYS AFTER Phase 2 (`d01912a` 2026-05-09) and Phase 3
             (`04cc350` 2026-05-09) E2E first-green-runs. STATE.md line 123 explicitly
             required pre-registration BEFORE green; this MP-2 anti-survivorship-bias
             guard is inverted. The verdict "stay-in-dogfood" survives on independent
             grounds but the pre-registration *claim* must be retracted.

  Plus 6 FLAGs:
    F1: README documents `telemetry-forget` only as a file-tree entry, not an invocable CLI.
    F2: Phases 4/5/6/7 have NO VERIFICATION.md — would have caught all 3 BLOCKers.
    F3: REQUIREMENTS.md traceability table all 41 still `[ ]` Pending.
    F4: `.github/workflows/install-linux-e2e.yml` exists but is not in ROADMAP / STATE.
    F5: `scripts/lib/telemetry.sh:15` website UUID is a placeholder
        `a1b2c3d4-e5f6-7890-abcd-ef1234567890` — opt-in telemetry will not reach a server.
    F6: `scripts/lib/telemetry-forget.sh:12` defaults FORGET_URL to localhost — end-users
        have no localhost server.

  7 PASSes confirmed: paths-filter non-overlapping, dict.yaml SHA single source-of-truth,
  telemetry opt-in non-blocking, telemetry payload allowlist clean of release-tag PII,
  install_id generation consistent cross-OS, release.yml atomic draft-then-publish,
  verify-librime.sh referenced consistently EXCEPT by install.sh (BLOCK-1).

  **Verdict: `gaps_found` — three BLOCKers + nine bookkeeping gaps.** The dogfood path
  works today on the founder's machine because the founder isn't a new user. The moment
  a non-founder (the GATE-02 wedge) runs `install.sh` on macOS or `install-windows.ps1`
  on Windows, BLOCK-1 (stale RESUME.md pointer) and BLOCK-2 (no uninstall flag) become
  user-visible. BLOCK-3 is a methodology defect, not a UX defect, but it undermines the
  decision-gate's claim of independence.

  Recommended sequence (~3-4 hours total):
    1. Fix BLOCK-1 (install.sh trailing message) + BLOCK-2 (Windows --uninstall) +
       FLAG-5 (real telemetry UUID) + FLAG-6 (production FORGET_URL) as a single
       Phase 6 follow-up commit. Re-run mac + win E2E.
    2. Address BLOCK-3 with caveat addendum to .planning/DECISION-GATE.md acknowledging
       the timing inversion; verdict stays.
    3. Backfill 04/05/06/07-VERIFICATION.md via `/gsd-verify-phase` (now the verifier
       can record both the closure AND the BLOCKer fixes).
    4. Flip the 41 REQUIREMENTS.md checkboxes; mark GATE-02 as "Deferred — pending
       non-founder recruits".
    5. Re-run `/gsd-audit-milestone v0.0.6`. Should pass.
    6. `/gsd-complete-milestone v0.0.6`.

gaps:
  requirements:
    - id: TELEM-01..09
      status: unsatisfied
      phase: "04-telemetry"
      claimed_by_plans: ["04-PLAN.md only — no 04-NN-PLAN.md files"]
      completed_by_plans: []
      verification_status: missing
      evidence: |
        Implementation present (infra/telemetry/docker-compose.yml, init.sql, Caddyfile,
        forget-api/, setup-website.sh, setup-triggers.sh, scripts/lib/telemetry.{sh,ps1},
        scripts/lib/telemetry-forget.{sh,ps1}, tests/test_telemetry.py). Commit 2a025cb
        + 3f252b2 close TELEM-06+09 + STATE update. No 04-VERIFICATION.md, no
        04-NN-SUMMARY.md, no VALIDATION.md. Trail of evidence is git-log + STATE.md only.
    - id: HARDEN-01,02,04,05,06,07
      status: partial
      phase: "05-sparkle-and-release-hardening"
      claimed_by_plans: ["05-01-PLAN.md", "05-02-PLAN.md"]
      completed_by_plans: ["05-01-SUMMARY.md", "05-02-SUMMARY.md"]
      verification_status: missing
      evidence: |
        SUMMARYs list each REQ-ID with implementation evidence (scripts/verify-librime.{sh,ps1},
        .github/workflows/release.yml, install-librime-fork.{sh,ps1} trailing messages, touch -m
        in install.sh). Test tag v0.0.6-test-release run 25649115808 GREEN per STATE.md.
        Missing: 05-VERIFICATION.md (gsd-verifier goal-backward pass).
    - id: HARDEN-03
      status: unsatisfied
      phase: "05-sparkle-and-release-hardening (cross-repo into smoodle-type/librime)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: missing
      evidence: |
        Cross-repo dependency: universal macOS dylib via `lipo -create` lives in
        smoodle-type/librime's smoodle-build.yml. Not in this repo's scope. Tracked as
        MP-3 in STATE.md (Pending). Audit cannot verify cross-repo; treat as
        out-of-scope rather than a v0.0.6 blocker.
    - id: DOCS-01..07
      status: unsatisfied
      phase: "06-readme-docs-hardening"
      claimed_by_plans: ["06-PLAN.md only"]
      completed_by_plans: []
      verification_status: missing
      evidence: |
        Implementation present: README.md has "Phase 1 status: APPROVED", smoodle-type org
        references (4 occurrences), 3-OS install/troubleshooting/uninstall sections,
        docs/RELEASE-CHECKLIST.md present, --uninstall flag in install.sh and
        install-linux.sh. Commits f6d2efb + 6814e32 + 0dc2ba6. Missing 06-VERIFICATION.md.
    - id: GATE-01,03,04
      status: unsatisfied
      phase: "07-decision-gate-close"
      claimed_by_plans: ["07-PLAN.md only"]
      completed_by_plans: []
      verification_status: missing
      evidence: |
        Artifacts exist: docs/DECISION-GATE-CRITERIA.md (pre-registered, GATE-01) +
        .planning/DECISION-GATE.md (filled checklist + verdict "stay-in-dogfood",
        GATE-03+04). Commits e1d1a7a + 2fdff4c. Missing 07-VERIFICATION.md confirming
        criteria pre-registration timestamp precedes Phase 2+3 green runs (the actual
        anti-survivorship-bias proof point).
    - id: GATE-02
      status: unsatisfied (intentionally deferred)
      phase: "07-decision-gate-close"
      claimed_by_plans: ["07-PLAN.md"]
      completed_by_plans: []
      verification_status: missing (cannot be completed by code alone)
      evidence: |
        7-day soak requires non-founder humans installing on their machines and reporting
        observations. As of audit date, zero non-founders attempted install. Verdict
        rendered as "stay-in-dogfood" precisely because GATE-02 cannot close from
        founder-only signal. NOT a code gap — this is the actual product-discovery
        gate the milestone exists to inform.
    - id: LINT-01, LINT-02
      status: partial
      phase: "01-lint-and-ci-fast-path"
      claimed_by_plans: ["01-01-PLAN.md"]
      completed_by_plans: ["01-01-SUMMARY.md (but frontmatter missing requirements_addressed)"]
      verification_status: passed
      evidence: |
        VERIFICATION.md confirms PASS for all 4 LINT REQ-IDs. 01-02-SUMMARY.md
        frontmatter correctly lists LINT-03+LINT-04. 01-01-SUMMARY.md frontmatter does
        NOT contain a requirements_addressed block — only inferable from VERIFICATION.
        Bookkeeping gap, not an implementation gap.

  integration:
    - id: BLOCK-1
      severity: BLOCK
      title: "install.sh trailing message points to stale docs/RESUME.md, not verify-librime.sh"
      affected_reqs: [HARDEN-06, DOCS-04, DOCS-07]
      evidence: "scripts/install.sh:216 final heredoc reads 'see docs/RESUME.md for the dylib-swap recipe.' Phase 5 commit b5f6919 updated install-librime-fork.sh:272 + install-librime-fork.ps1:473 to reference verify-librime.sh but missed the primary user-facing installer."
      closure: "Edit scripts/install.sh lines 212-216 to reference 'bash scripts/verify-librime.sh' mirroring install-librime-fork.sh:272. Re-run mac E2E."
    - id: BLOCK-2
      severity: BLOCK
      title: "install-windows.ps1 has zero --uninstall flag matches"
      affected_reqs: [DOCS-04]
      evidence: "`grep -ni uninstall scripts/install-windows.ps1` returns 0. install.sh:21 + install-linux.sh:26 both implement --uninstall. STATE.md line 33 'uninstall flags added' is only 2 of 3 OSes."
      closure: "Port the --uninstall block from install-linux.sh:25-72 into install-windows.ps1 (remove %APPDATA%\\Rime\\thai_phonetic.*.yaml + default.custom.yaml + $env:USERPROFILE\\.smoodle\\). Re-run win E2E."
    - id: BLOCK-3
      severity: BLOCK
      title: "GATE-01 pre-registration committed AFTER Phase 2/3 first green runs (anti-survivorship-bias guard inverted)"
      affected_reqs: [GATE-01, MP-2]
      evidence: "git log: docs/DECISION-GATE-CRITERIA.md first commit e1d1a7a 2026-05-11 14:27. Phase 2 mac E2E merged d01912a 2026-05-09 11:25. Phase 3 win E2E merged 04cc350 2026-05-09 15:52. STATE.md line 123 explicit: 'GATE-01 pre-registration must be authored *before* Phase 2/3 turn green.'"
      closure: "Edit .planning/DECISION-GATE.md to add 'Pre-registration timing caveat' section explicitly acknowledging MP-2 was not mitigated as planned; cite git timestamps. Verdict 'stay-in-dogfood' survives on independent grounds."
    - id: FLAG-1
      severity: FLAG
      title: "README documents telemetry-forget.{sh,ps1} only as file-tree entries, not as CLI"
      affected_reqs: [TELEM-06]
      evidence: "`grep -n 'telemetry forget\\|telemetry-forget' README.md` returns only file-tree lines 147-148. TELEM-06 says '`smoodle telemetry forget` CLI ... documented in README.'"
      closure: "Add a Telemetry/Privacy subsection to README with verbatim invocations of telemetry-forget.{sh,ps1}."
    - id: FLAG-5
      severity: FLAG
      title: "Telemetry website UUID is a hardcoded placeholder, not a real umami site_id"
      affected_reqs: [TELEM-01, TELEM-02, TELEM-03]
      evidence: "scripts/lib/telemetry.sh:15 — SMOODLE_TELEMETRY_WEBSITE=\"${SMOODLE_TELEMETRY_WEBSITE:-a1b2c3d4-e5f6-7890-abcd-ef1234567890}\". If anyone opts in today, payload references a non-existent UUID — server rejects event. STATE.md line 117 'smoke-tested end-to-end' cannot have succeeded against this."
      closure: "Swap in real umami website_id (founder picks) OR mark TELEM-01 as Deferred-pending-infra in REQUIREMENTS.md."
    - id: FLAG-6
      severity: FLAG
      title: "telemetry-forget client points to http://localhost:8080/api/forget by default"
      affected_reqs: [TELEM-06]
      evidence: "scripts/lib/telemetry-forget.sh:12 + telemetry-forget.ps1:12 both default FORGET_URL to http://localhost:8080/api/forget. End-users will not have a forget-api container on localhost."
      closure: "Change default to https://telemetry.0dl.me/api/forget once subdomain is live; document SMOODLE_FORGET_URL override in README."
    - id: FLAG-4
      severity: FLAG
      title: "Linux E2E workflow exists but not in STATE.md / ROADMAP.md phase manifest"
      affected_reqs: []
      evidence: ".github/workflows/install-linux-e2e.yml exists and runs install-linux.sh on paths-filter. Not listed in ROADMAP.md phase table. Either silent scope-creep or untracked Phase 1.5 prework."
      closure: "Either delete (deferral per CLAUDE.md) OR add a one-line STATE.md note disclosing as out-of-scope bonus coverage."

  flows: []  # E2E flows verified per-phase (mac run 25594460125, win run 25623956809, release run 25649115808). End-user dogfood flow is broken at BLOCK-1 + BLOCK-2 boundaries — new user on macOS sees stale RESUME.md pointer; new user on Windows has no uninstall path.

tech_debt:
  - phase: "01-lint-and-ci-fast-path"
    items:
      - "01-01-SUMMARY.md missing `requirements_addressed` frontmatter (LINT-01, LINT-02 only inferable via VERIFICATION.md)"
  - phase: "04-telemetry"
    items:
      - "No 04-NN-PLAN.md files (single 04-PLAN.md only; 3 waves done in-place per STATE)"
      - "No 04-NN-SUMMARY.md files (trail is git log + STATE.md only)"
      - "No 04-VERIFICATION.md (gsd-verifier never ran)"
      - "No 04-VALIDATION.md (Nyquist)"
  - phase: "05-sparkle-and-release-hardening"
    items:
      - "No 05-VERIFICATION.md (despite 05-01-SUMMARY.md + 05-02-SUMMARY.md existing)"
      - "No 05-VALIDATION.md (Nyquist)"
  - phase: "06-readme-docs-hardening"
    items:
      - "No 06-NN-PLAN.md / 06-NN-SUMMARY.md (single 06-PLAN.md only)"
      - "No 06-VERIFICATION.md"
      - "No 06-VALIDATION.md (Nyquist)"
  - phase: "07-decision-gate-close"
    items:
      - "No 07-NN-PLAN.md / 07-NN-SUMMARY.md (single 07-PLAN.md only)"
      - "No 07-VERIFICATION.md (criteria pre-registration timestamp NOT formally proven against Phase 2/3 green-run timestamps)"
      - "No 07-VALIDATION.md (Nyquist)"
  - phase: "all"
    items:
      - "REQUIREMENTS.md traceability table: all 41 REQ-IDs still marked `- [ ]` + status \"Pending\". Source-of-truth never flipped post-closure."

nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: ["01", "02", "03", "04", "05", "06", "07"]
  overall: missing-all

cross_repo:
  - dep: "HARDEN-03 universal dylib via lipo -create"
    upstream: "smoodle-type/librime"
    status: "Pending — tracked as MP-3 in STATE.md. Out of scope for this repo's audit; do not let block v0.0.6 close."
---

# Milestone v0.0.6 (phase-1-finish) — Audit Report

**Audited:** 2026-05-16
**Status:** `gaps_found`
**Verdict line:** Implementation materially complete; verification artifact set incomplete. Code-on-disk passes the eye test for all 7 phases; the gsd-workflow's required `VERIFICATION.md` / `VALIDATION.md` / traceability-checkbox-flip trail does not.

## Score Summary

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements satisfied | **12 / 41** | + 8 partial + 21 unsatisfied. See FAIL gate below. |
| Phase verifications | **3 / 7** | Phases 1, 2, 3 have VERIFICATION.md. Phase 5 has SUMMARYs but no VERIFICATION. Phases 4, 6, 7 have only a PLAN and STATE-level commit trail. |
| Phase SUMMARYs | **5 / 7** | Phases 1 (×2 plans), 2 (×2), 3 (×1), 5 (×2). Phases 4, 6, 7: zero. |
| Nyquist VALIDATION | **0 / 7** | All MISSING. `workflow.nyquist_validation: true` is enabled. |
| Integration | pending | Background agent dispatched; report at `.planning/INTEGRATION-CHECK-v0.0.6.md`. |

## Phase-by-phase artifact matrix

| Phase | PLAN.md | SUMMARY.md | VERIFICATION.md | VALIDATION.md | REQ-IDs in REQUIREMENTS.md checked | Live evidence |
|-------|---------|------------|-----------------|---------------|-------|---------------|
| 01 LINT | ✓ (×2) | ✓ (×2) — but 01-01 missing requirements frontmatter | ✓ PASS | ✗ | 0/4 | CI runs 25589240363 (green) + 25589248240 (red, correct reason) + 25589253046 (red, correct reason) |
| 02 E2EMAC | ✓ (×2) | ✓ (×2) | ✓ PASS | ✗ | 0/5 | macos-15 run 25594460125 GREEN 1m4s |
| 03 E2EWIN | ✓ | ✓ | ✓ PASS | ✗ | 0/5 | windows-latest run 25623956809 GREEN 2m12s |
| 04 TELEM | ✓ (single) | ✗ | ✗ | ✗ | 0/9 | None — git-log trail only |
| 05 HARDEN | ✓ (×2) | ✓ (×2) | ✗ | ✗ | 0/7 | release.yml run 25649115808 GREEN (test tag) |
| 06 DOCS | ✓ (single) | ✗ | ✗ | ✗ | 0/7 | None — disk inspection + git-log only |
| 07 GATE | ✓ (single) | ✗ | ✗ | ✗ | 0/4 | DECISION-GATE-CRITERIA.md + DECISION-GATE.md authored |

## Unsatisfied requirements (FAIL gate forces `gaps_found`)

### Pure bookkeeping (implementation present, just unverified by gsd-verifier)

- **TELEM-01..09** (9): infra + clients + forget CLI + tests on disk; SHIPPED-via-git but no `04-VERIFICATION.md` PASS pass.
- **HARDEN-01, 02, 04, 05, 06, 07** (6): SUMMARYs PASS evidence per REQ-ID; missing the goal-backward `05-VERIFICATION.md` rollup.
- **DOCS-01..07** (7): README, RELEASE-CHECKLIST, --uninstall flags all on disk; missing `06-VERIFICATION.md`.
- **GATE-01, 03, 04** (3): pre-registration + checklist + verdict on disk; missing `07-VERIFICATION.md`.

### Genuinely unfinished (not closeable by code)

- **GATE-02** (1): 7-day soak with non-founders. **This is the whole point of the milestone's existence** — the verdict "stay-in-dogfood" is the system telling you GATE-02 cannot close until recruitment happens.

### Cross-repo dependency

- **HARDEN-03** (1): universal dylib lipo -create in `smoodle-type/librime`. Out of scope here; track in upstream repo.

## Tech debt summary

22 bookkeeping items across phases 4, 5, 6, 7 + REQUIREMENTS.md table + 01-01 frontmatter + 0/7 Nyquist VALIDATION coverage. None block the implementation; all block clean milestone-completion via `/gsd-complete-milestone`.

## Recommendations

**Two legitimate paths forward.**

### Path A — Fix BLOCKers + close bookkeeping (recommended, ~3-4 hours)

The integration check changed the calculus. This is no longer pure bookkeeping — three real cross-phase wiring failures must be fixed before the next user installs.

**Step 1: Fix BLOCK-1 + BLOCK-2 + telemetry deployment (FLAG-5, FLAG-6).** Single Phase 6 follow-up commit. About 60-90 min.
- Edit `scripts/install.sh:212-216` → replace `docs/RESUME.md` reference with `bash scripts/verify-librime.sh` (mirror `install-librime-fork.sh:272`).
- Port `--uninstall` block from `install-linux.sh:25-72` into `install-windows.ps1`.
- Replace placeholder website UUID in `scripts/lib/telemetry.sh:15` + `telemetry.ps1` with real umami site_id (or mark TELEM-01 as Deferred-pending-infra).
- Change `FORGET_URL` default in `telemetry-forget.{sh,ps1}:12` to production endpoint + document `SMOODLE_FORGET_URL` override in README.
- Re-run mac E2E + win E2E (manual `workflow_dispatch`).

**Step 2: Address BLOCK-3 with caveat addendum.** ~10 min.
- Edit `.planning/DECISION-GATE.md` — add a "Pre-registration timing caveat" section citing the e1d1a7a / d01912a / 04cc350 git timestamps. Verdict `stay-in-dogfood` survives on independent grounds; the MP-2 mitigation claim is retracted.

**Step 3: Backfill verification artifacts.** ~30-60 min.
```bash
/gsd-verify-phase 4   # writes 04-VERIFICATION.md absorbing telemetry fixes
/gsd-verify-phase 5   # writes 05-VERIFICATION.md
/gsd-verify-phase 6   # writes 06-VERIFICATION.md absorbing install.sh + Windows uninstall fixes
/gsd-verify-phase 7   # writes 07-VERIFICATION.md absorbing GATE-01 timing caveat
```

**Step 4: Flip the 41 REQUIREMENTS.md checkboxes.** ~10 min.
- Bulk update column 3 to reflect completed status.
- Mark `GATE-02` as `Deferred — pending non-founder recruits` (not Pending).
- Mark BLOCK-affected REQs (HARDEN-06, DOCS-04, DOCS-07, GATE-01) appropriately based on Step 1-2 fixes.

**Step 5: Re-audit + complete.**
```bash
/gsd-audit-milestone v0.0.6   # should now pass
/gsd-complete-milestone v0.0.6
```

### Path B — Accept tech debt + force-complete

Defensible if you're confident no non-founder will install in the next few days AND recruitment is genuinely the higher-priority activity. Risks: BLOCK-1 + BLOCK-2 become user-visible the moment a non-founder runs `install.sh` or `install-windows.ps1`. BLOCK-3 is a methodology defect that the milestone post-mortem will need to record honestly.

```bash
/gsd-complete-milestone v0.0.6   # accepts the gaps; document in CHANGELOG
```

Recommended only if you treat the BLOCKers as "Phase 1.5 day-1 hotfixes" and prioritize them above any new milestone work.

### Path C — Defer everything (NOT recommended)

`stay-in-dogfood` is technically already the verdict. Walk away, start recruiting, accept that the first non-founder will hit BLOCK-1 + BLOCK-2 + FLAG-5/6 directly. Only justifiable if you have a private direct-channel to the first 2-5 recruits and can walk them through manually.

## Integration checker — landed

**Report:** `.planning/INTEGRATION-CHECK-v0.0.6.md` (170 lines)
**Verdict:** 3 BLOCK · 6 FLAG · 7 PASS

### BLOCKers (must fix before any non-founder install attempt)

1. **BLOCK-1** — `scripts/install.sh:216` trailing message still points to stale `docs/RESUME.md`, not `verify-librime.sh`. Phase 5 fixed `install-librime-fork.{sh,ps1}` (commits `b5f6919`, `e71a2ad`) but missed the *primary* user-facing installer. New users see the wrong recovery path. Affects HARDEN-06, DOCS-04, DOCS-07.
2. **BLOCK-2** — `scripts/install-windows.ps1` has **zero `--uninstall` matches**. mac + linux installers both implement it. STATE.md claims "uninstall flags added" — that's only 2 of 3 OSes. Affects DOCS-04 (3-OS parity claim is false).
3. **BLOCK-3** — `docs/DECISION-GATE-CRITERIA.md` first commit `e1d1a7a` is dated `2026-05-11 14:27` — **2 days AFTER** Phase 2 green (`d01912a` 2026-05-09) and Phase 3 green (`04cc350` 2026-05-09). STATE.md line 123 explicitly required pre-registration *before* green. The anti-MP-2 survivorship-bias guard is inverted. Verdict `stay-in-dogfood` survives on independent grounds, but the pre-registration *claim* must be retracted in `.planning/DECISION-GATE.md` with a timing-caveat section.

### FLAGs (telemetry deployment incomplete, docs incomplete)

- **FLAG-1** — README documents `telemetry-forget.{sh,ps1}` only as file-tree entries, not as an invocable CLI. TELEM-06 says "CLI documented in README" — that's false.
- **FLAG-2** — Phases 4, 5, 6, 7 have no `*-VERIFICATION.md` (would have caught all 3 BLOCKers above).
- **FLAG-3** — All 41 REQ-IDs in `REQUIREMENTS.md` still `[ ]` Pending despite STATE.md claiming closure.
- **FLAG-4** — `.github/workflows/install-linux-e2e.yml` exists but is not in ROADMAP.md / STATE.md phase manifest — silent scope-creep or untracked prework.
- **FLAG-5** — `scripts/lib/telemetry.sh:15` website UUID is a placeholder `a1b2c3d4-e5f6-7890-abcd-ef1234567890`. If anyone opts in today, server rejects the event. TELEM-01 "smoke-tested end-to-end" cannot have succeeded against this.
- **FLAG-6** — `scripts/lib/telemetry-forget.{sh,ps1}:12` defaults `FORGET_URL` to `http://localhost:8080/api/forget`. End-users have no localhost server.

### PASSes (the parts that work)

1. ci.yml + mac-e2e.yml + win-e2e.yml paths-filters are non-overlapping by design.
2. Phase 2 + Phase 3 share `schema/thai_phonetic.dict.yaml` as single SHA source-of-truth.
3. Phase 4 telemetry opt-in is non-blocking for installer completion.
4. Telemetry payload allowlist correctly excludes release-tag PII (hardcoded `0.0.6`).
5. `install_id` generation is consistent across mac + linux clients (`sha256` of 16 urandom bytes).
6. `release.yml` uses atomic draft-then-publish + tag-immutability assertion; live-verified.
7. `verify-librime.sh` referenced consistently by `install-librime-fork.{sh,ps1}` + `RELEASE-CHECKLIST.md` — **except** missing from `install.sh` (BLOCK-1).

## Notes for the next milestone

The pattern that broke is clear: **after Phase 3, verification discipline tapered off**. Phases 1-3 followed the full GSD chain (PLAN → SUMMARY → VERIFICATION). Phases 4-7 collapsed to PLAN-only + STATE.md commit-as-closure. This is a process drift, not a code-quality issue — the work itself looks solid.

For the next milestone, either:
1. Re-commit to the full chain on every phase (and rebuild the verifier-running habit), or
2. Adopt an explicit "lite" mode in `.planning/config.json` so the audit knows STATE-only closure is the policy and doesn't fail-gate on missing VERIFICATIONs.

The current state — full chain expected but optional in practice — is the worst-of-both.

---
*Generated by `/gsd-progress --next` → `/gsd-audit-milestone` flow on 2026-05-16.*
